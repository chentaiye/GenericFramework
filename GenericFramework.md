# GenericFramework Skill Archive

`GenericFramework.md` is retired from the default skill workflow.

## Current Structure
- `.codex/skills` contains the `auto-assistant` dispatcher, standard non-UE skills, and project-neutral Unreal Engine skills.
- `.genericframework/skills` contains GenericFramework-only workspace, C++ style, and API policy rules.

## Usage
- Do not treat this file as a rule source.
- Use `ue-auto-assistant` for staged routing.
- Read the specific skill files selected for the current task stage.

## Retention
- This file remains only as a historical note for the old centralized rule document.
