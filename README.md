# GenericFramework

This repository contains long Windows paths and large Git LFS files.

On Windows, clone with the Git LFS workflow in [docs/git-clone.md](docs/git-clone.md) to avoid long-path checkout errors and LFS download timeouts.
